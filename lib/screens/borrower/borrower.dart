import 'package:flutter/material.dart';

class Borrower extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Borrower Detail')),
      body: Center(child: Text('Borrower detail page')),
    );
  }
}
